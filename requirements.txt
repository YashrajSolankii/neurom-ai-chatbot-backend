# requirements.txt

# --- Core API framework (main_api.py) ---
fastapi>=0.104.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.9
pydantic>=2.0.0
python-dotenv>=1.0.0
setuptools>=65.5.1

# --- Claude API (core_logic.py) ---
anthropic>=0.34.0

# --- RAG / vector store (core_logic.py) ---
langchain>=0.1.0
langchain-community>=0.0.20
langchain-chroma>=0.1.0
chromadb>=0.4.22
duckdb>=0.9.2
duckdb-engine>=0.9.0
protobuf==3.20.3
pypdf>=3.17.0

# --- Embeddings + emotion classifier (core_logic.py) ---
sentence-transformers>=2.2.2
transformers>=4.30.0
torch>=2.0.0

# --- Voice transcription (voice_service.py) ---
openai-whisper>=20231117

# --- Legacy / leftover from earlier Gemini-based version ---
# Not used by the current Claude-based core_logic.py — safe to remove later,
# kept for now in case other parts of the project still reference them.
langchain-google-genai>=0.0.9
google-generativeai>=0.4.0
